---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - bookforge/__init__.py
  - bookforge/cli/__init__.py
  - bookforge/story/__init__.py
  - bookforge/story/schema.py
  - bookforge/style/__init__.py
  - bookforge/style/schema.py
  - bookforge/state.py
  - bookforge/config.py
  - tests/conftest.py
  - tests/__init__.py
autonomous: true
requirements:
  - STOR-01
  - STOR-06
  - STYL-01
  - STYL-02
  - STYL-05

must_haves:
  truths:
    - "uv run bookforge --help prints CLI help without errors"
    - "BookMeta, Page, BilingualText, Book Pydantic models validate correct data and reject invalid data"
    - "StyleGuide, CharacterDef, ArtStyle, ImageConfig Pydantic models validate correct data and reject invalid data"
    - "Test fixtures provide sample story.md and style guide YAML for downstream test files"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project config with bookforge entry point and all Phase 1 dependencies"
      contains: "bookforge.cli:app"
    - path: "bookforge/story/schema.py"
      provides: "Book, BookMeta, Page, BilingualText Pydantic models"
      exports: ["Book", "BookMeta", "Page", "BilingualText"]
    - path: "bookforge/style/schema.py"
      provides: "StyleGuide, CharacterDef, ArtStyle, ImageConfig, Layout Pydantic models"
      exports: ["StyleGuide", "CharacterDef", "ArtStyle", "ImageConfig", "Layout"]
    - path: "bookforge/config.py"
      provides: "Env var loading for ANTHROPIC_API_KEY"
    - path: "tests/conftest.py"
      provides: "Shared fixtures for all test files"
  key_links:
    - from: "bookforge/cli/__init__.py"
      to: "pyproject.toml"
      via: "project.scripts entry point"
      pattern: 'bookforge = "bookforge.cli:app"'
---

<objective>
Set up the entire Python project from scratch: pyproject.toml with all Phase 1 dependencies, the bookforge package structure, all Pydantic data models (story + style), config module, state module skeleton, and shared test fixtures.

Purpose: Every subsequent plan depends on the package being installable and the data models existing. This is the shared foundation.
Output: Installable bookforge package with `uv run bookforge --help` working, all Pydantic schemas defined, test fixtures ready.
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pyproject.toml, package structure, and Pydantic schemas</name>
  <files>
    pyproject.toml
    bookforge/__init__.py
    bookforge/cli/__init__.py
    bookforge/story/__init__.py
    bookforge/story/schema.py
    bookforge/style/__init__.py
    bookforge/style/schema.py
    bookforge/state.py
    bookforge/config.py
    tests/__init__.py
    tests/conftest.py
  </files>
  <behavior>
    - Test: BookMeta(**valid_frontmatter_dict) succeeds; BookMeta with missing title raises ValidationError
    - Test: Page with number, BilingualText, image_prompt validates; Page with empty image_prompt raises ValidationError
    - Test: Page.effective_prompt() returns image_override when set, image_prompt when not
    - Test: Book with empty pages list raises ValidationError ("must have at least one page")
    - Test: StyleGuide(**valid_style_dict) succeeds; StyleGuide with missing art_style raises ValidationError
    - Test: CharacterDef with name_en, name_ko, description, reference_image validates
    - Test: StyleGuide.build_prompt_prefix() returns string containing art_style.prompt_prefix and character descriptions
    - Test: ImageConfig defaults provider to "flux_kontext_pro"
  </behavior>
  <action>
    1. Create pyproject.toml:
       - name: bookforge, version: 0.1.0, requires-python: ">=3.11"
       - dependencies: typer>=0.24.1, rich>=14.3.3, anthropic>=0.86.0, python-frontmatter>=1.1.0, ruamel.yaml>=0.19.1, pydantic>=2.12.5
       - dev dependencies: pytest, pytest-asyncio, respx
       - [project.scripts] bookforge = "bookforge.cli:app"
       - [build-system] hatchling
       - [tool.pytest.ini_options] testpaths = ["tests"]

    2. Create package structure with __init__.py files (bookforge/, bookforge/cli/, bookforge/story/, bookforge/style/, tests/)

    3. Create bookforge/cli/__init__.py:
       - Typer app with name="bookforge", help text, no_args_is_help=True
       - No subcommands yet (those come in Plan 04)

    4. Create bookforge/story/schema.py with Pydantic v2 models exactly as specified in RESEARCH.md Pattern 2:
       - BilingualText(en: str, ko: str)
       - Page(number: int, text: BilingualText, image_prompt: str, image_override: str | None = None) with effective_prompt() method
       - BookMeta(title: str, title_ko: str, slug: str, trim_size: str = "8.5x8.5", price: float, ages: str, style_guide: str)
       - Book(meta: BookMeta, pages: list[Page]) with @field_validator ensuring non-empty pages

    5. Create bookforge/style/schema.py with Pydantic v2 models exactly as specified in RESEARCH.md Pattern 3:
       - CharacterDef(name_en: str, name_ko: str, description: str, reference_image: str)
       - ArtStyle(prompt_prefix: str, negative_prompt: str, color_palette: list[str])
       - ImageConfig(provider: str = "flux_kontext_pro", width: int = 1024, height: int = 1024)
       - Layout(trim_inches: list[float] = [8.5, 8.5], bleed_inches: float = 0.125, safe_margin_inches: float = 0.25, dpi: int = 300)
       - StyleGuide(name: str, version: int = 1, art_style: ArtStyle, image: ImageConfig = ImageConfig(), characters: dict[str, CharacterDef], layout: Layout = Layout()) with build_prompt_prefix() method

    6. Create bookforge/state.py with load_state() and save_state() using atomic write pattern (RESEARCH.md Pattern 5)

    7. Create bookforge/config.py:
       - get_anthropic_key() that reads ANTHROPIC_API_KEY from env, raises clear error via typer.echo + typer.Exit(1) if missing
       - BOOKFORGE_MODEL env var with default "claude-sonnet-4-5"

    8. Create tests/conftest.py with shared fixtures:
       - sample_story_md: string fixture with 2-page sample story in canonical format
       - sample_story_file: writes sample_story_md to tmp_path, returns Path
       - sample_style_dict: dict matching StyleGuide schema (with ho-rang and gom-i characters)
       - sample_style_yaml: writes sample_style_dict as YAML to tmp_path, returns Path
       - book_dir: creates a tmp book directory with subdirs (images/, dist/, publish/)

    9. Run `uv sync` to install all dependencies

    10. Write tests in tests/test_schemas.py covering all behavior items above

    11. Run tests -- all must pass
  </action>
  <verify>
    <automated>uv run bookforge --help && uv run pytest tests/test_schemas.py -x -v</automated>
  </verify>
  <done>
    - pyproject.toml exists with correct deps and entry point
    - `uv run bookforge --help` prints help text
    - All Pydantic schema tests pass (BookMeta, Page, Book, StyleGuide, CharacterDef validation)
    - tests/conftest.py provides fixtures used by subsequent plans
  </done>
</task>

</tasks>

<verification>
- `uv run bookforge --help` exits 0 and shows "bookforge" in output
- `uv run pytest tests/ -x -v` passes all schema tests
- All Pydantic models importable: `uv run python -c "from bookforge.story.schema import Book; from bookforge.style.schema import StyleGuide; print('OK')"`
</verification>

<success_criteria>
- Installable Python package with working CLI entry point
- All Pydantic data models defined and tested
- Shared test fixtures available for Plans 02-04
- Zero test failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
