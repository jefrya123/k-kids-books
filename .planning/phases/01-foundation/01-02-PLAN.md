---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - bookforge/story/parser.py
  - bookforge/story/validator.py
  - tests/test_story_parser.py
autonomous: true
requirements:
  - STOR-02
  - STOR-03
  - STOR-04

must_haves:
  truths:
    - "A well-formed story.md parses into a Book with correct metadata and pages"
    - "English text is extracted without Korean or image comments mixed in"
    - "Korean text is extracted from ko comment blocks"
    - "Image prompts are extracted from image comment blocks"
    - "Image overrides are extracted when present, None when absent"
    - "Parser rejects story.md with missing frontmatter fields"
    - "Validator reports pages missing Korean text or image prompts"
  artifacts:
    - path: "bookforge/story/parser.py"
      provides: "parse_story(path) -> Book function"
      exports: ["parse_story"]
      min_lines: 40
    - path: "bookforge/story/validator.py"
      provides: "validate_book(book) -> list[str] returning warning messages"
      exports: ["validate_book"]
    - path: "tests/test_story_parser.py"
      provides: "Unit tests for STOR-01 through STOR-06"
      min_lines: 60
  key_links:
    - from: "bookforge/story/parser.py"
      to: "bookforge/story/schema.py"
      via: "imports Book, BookMeta, Page, BilingualText"
      pattern: "from bookforge.story.schema import"
    - from: "bookforge/story/parser.py"
      to: "python-frontmatter"
      via: "frontmatter.load() for YAML header"
      pattern: "frontmatter.load"
---

<objective>
Implement the story.md parser that turns a markdown file with YAML frontmatter, `## Page N` headers, bilingual text blocks, and image prompts into a validated Book Pydantic model. Also implement a validator that checks completeness.

Purpose: The parser is the core data pipeline -- every downstream command (illustrate, build, publish) depends on parsing story.md into structured data.
Output: Working parse_story() function with full test coverage.
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 outputs -- executor needs these types -->

From bookforge/story/schema.py:
```python
class BilingualText(BaseModel):
    en: str
    ko: str

class Page(BaseModel):
    number: int
    text: BilingualText
    image_prompt: str
    image_override: str | None = None
    def effective_prompt(self) -> str: ...

class BookMeta(BaseModel):
    title: str
    title_ko: str
    slug: str
    trim_size: str = "8.5x8.5"
    price: float
    ages: str
    style_guide: str

class Book(BaseModel):
    meta: BookMeta
    pages: list[Page]
```

From tests/conftest.py:
```python
# Fixtures available: sample_story_md, sample_story_file, sample_style_dict, sample_style_yaml, book_dir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement story parser with targeted comment extraction</name>
  <files>bookforge/story/parser.py, tests/test_story_parser.py</files>
  <behavior>
    - Test: parse_story on 2-page sample returns Book with meta.slug matching frontmatter
    - Test: parse_story splits pages correctly -- 2 pages in sample yields len(book.pages) == 2
    - Test: Page 1 English text contains only English, no Korean or HTML comments
    - Test: Page 1 Korean text extracted correctly from ko block
    - Test: Page 1 image_prompt extracted from image comment
    - Test: Page with image-override comment has image_override set; page without has None
    - Test: effective_prompt() returns override when present
    - Test: English text containing regular HTML comments (not ko/image) is preserved (Pitfall 1 from research)
    - Test: Parse fails with ValidationError on missing frontmatter field (e.g. no title)
    - Test: Parse handles multi-line image prompts correctly
  </behavior>
  <action>
    Create bookforge/story/parser.py following RESEARCH.md Pattern 4 with this critical fix from Pitfall 1:

    The _extract_en() function must NOT strip all HTML comments blindly. Instead:
    1. Remove only known structured comments: `<!-- ko -->...<!-- /ko -->` blocks, `<!-- image: ... -->`, `<!-- image-override: ... -->`
    2. Preserve any other HTML comments that might appear in English prose

    Implementation:
    - parse_story(path: str | Path) -> Book
    - Uses python-frontmatter to load YAML header -> BookMeta
    - Splits body on `re.split(r'^## Page \d+\s*$', content, flags=re.MULTILINE)`
    - For each section, extracts: _extract_en(), _extract_ko(), _extract_image(), _extract_image_override()
    - Returns Book(meta=meta, pages=pages)

    Write tests in tests/test_story_parser.py using conftest fixtures.
  </action>
  <verify>
    <automated>uv run pytest tests/test_story_parser.py -x -v</automated>
  </verify>
  <done>parse_story() correctly parses sample story.md into Book model; all edge cases tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement story validator for completeness checks</name>
  <files>bookforge/story/validator.py, tests/test_story_parser.py</files>
  <behavior>
    - Test: validate_book on complete book returns empty list
    - Test: validate_book on book with page missing Korean text returns warning mentioning page number
    - Test: validate_book on book with page missing image prompt returns warning
    - Test: validate_book on book with 0 pages returns error (though Pydantic already catches this)
  </behavior>
  <action>
    Create bookforge/story/validator.py:
    - validate_book(book: Book) -> list[str] that returns a list of warning/error strings
    - Checks each page has non-empty en text, ko text, and image_prompt
    - Returns descriptive messages like "Page 3: missing Korean text"
    - This is a soft validator (warnings, not exceptions) -- used by the CLI to show issues to the user

    Add validator tests to the existing tests/test_story_parser.py file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_story_parser.py -x -v</automated>
  </verify>
  <done>validate_book() catches missing content per page and returns descriptive warnings</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_story_parser.py -x -v` -- all tests green
- `uv run pytest tests/ -x -q` -- full suite still passes (no regressions)
</verification>

<success_criteria>
- parse_story() parses canonical story.md format into validated Book model
- English extraction does not strip non-structural HTML comments (Pitfall 1 addressed)
- validate_book() reports completeness issues per page
- All parser and validator tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
