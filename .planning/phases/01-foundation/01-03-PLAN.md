---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - bookforge/style/loader.py
  - style-guides/korean-cute-watercolor.yaml
  - style-guides/characters/.gitkeep
  - tests/test_style_loader.py
autonomous: true
requirements:
  - STYL-03
  - STYL-04

must_haves:
  truths:
    - "Style guide YAML loads into validated StyleGuide Pydantic model"
    - "Character definitions include Ho-rang and Gom-i with reference image paths"
    - "build_prompt_prefix() assembles style prefix + character descriptions + negative prompt"
    - "Default style guide YAML is valid and loads without error"
    - "Reference image paths in style guide point to expected locations"
  artifacts:
    - path: "bookforge/style/loader.py"
      provides: "load_style_guide(path) -> StyleGuide function"
      exports: ["load_style_guide"]
    - path: "style-guides/korean-cute-watercolor.yaml"
      provides: "Default style guide with Ho-rang and Gom-i character definitions"
      contains: "ho-rang"
    - path: "tests/test_style_loader.py"
      provides: "Tests for STYL-01 through STYL-05"
      min_lines: 50
  key_links:
    - from: "bookforge/style/loader.py"
      to: "bookforge/style/schema.py"
      via: "imports StyleGuide for validation"
      pattern: "from bookforge.style.schema import StyleGuide"
    - from: "bookforge/style/loader.py"
      to: "ruamel.yaml"
      via: "YAML round-trip loader"
      pattern: "from ruamel.yaml import YAML"
    - from: "style-guides/korean-cute-watercolor.yaml"
      to: "style-guides/characters/"
      via: "reference_image paths"
      pattern: "characters/"
---

<objective>
Implement the style guide YAML loader using ruamel.yaml + Pydantic validation, create the default Korean watercolor style guide with Ho-rang and Gom-i character definitions, and set up the characters/ directory for reference images.

Purpose: The style guide drives all image generation in Phase 2 -- prompt prefixes, character descriptions, and reference image paths must be defined and validated here.
Output: Working load_style_guide() function, default style guide YAML, full test coverage.
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 outputs -->

From bookforge/style/schema.py:
```python
class CharacterDef(BaseModel):
    name_en: str
    name_ko: str
    description: str
    reference_image: str

class ArtStyle(BaseModel):
    prompt_prefix: str
    negative_prompt: str
    color_palette: list[str]

class ImageConfig(BaseModel):
    provider: str = "flux_kontext_pro"
    width: int = 1024
    height: int = 1024

class Layout(BaseModel):
    trim_inches: list[float] = [8.5, 8.5]
    bleed_inches: float = 0.125
    safe_margin_inches: float = 0.25
    dpi: int = 300

class StyleGuide(BaseModel):
    name: str
    version: int = 1
    art_style: ArtStyle
    image: ImageConfig = ImageConfig()
    characters: dict[str, CharacterDef]
    layout: Layout = Layout()
    def build_prompt_prefix(self) -> str: ...
```

From tests/conftest.py:
```python
# Fixtures: sample_style_dict, sample_style_yaml
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement style guide loader with ruamel.yaml</name>
  <files>bookforge/style/loader.py, tests/test_style_loader.py</files>
  <behavior>
    - Test: load_style_guide on valid YAML returns StyleGuide with correct name
    - Test: load_style_guide loads characters dict with ho-rang and gom-i keys
    - Test: CharacterDef for ho-rang has name_en, name_ko, description, reference_image
    - Test: StyleGuide.image.provider defaults to "flux_kontext_pro"
    - Test: build_prompt_prefix() returns string containing prompt_prefix text
    - Test: build_prompt_prefix() returns string containing character description text
    - Test: build_prompt_prefix() returns string containing negative prompt
    - Test: load_style_guide raises error on YAML with missing required field (art_style)
    - Test: load_style_guide raises error on non-existent file path
  </behavior>
  <action>
    Create bookforge/style/loader.py following RESEARCH.md code example:
    - load_style_guide(path: Path) -> StyleGuide
    - Uses ruamel.yaml YAML() with preserve_quotes=True
    - Converts ruamel CommentedMap to plain dict via json round-trip (Pitfall 2: never serialize Pydantic back through ruamel)
    - Validates through StyleGuide(**plain_dict)
    - Raises clear error if file not found or validation fails

    Write tests in tests/test_style_loader.py using conftest fixtures.
  </action>
  <verify>
    <automated>uv run pytest tests/test_style_loader.py -x -v</automated>
  </verify>
  <done>load_style_guide() loads YAML, validates via Pydantic, returns StyleGuide model</done>
</task>

<task type="auto">
  <name>Task 2: Create default style guide YAML and characters directory</name>
  <files>style-guides/korean-cute-watercolor.yaml, style-guides/characters/.gitkeep</files>
  <action>
    1. Create style-guides/characters/.gitkeep (empty file to track directory in git; actual reference PNGs generated in Phase 2)

    2. Create style-guides/korean-cute-watercolor.yaml as a hand-written YAML file (NOT generated from Pydantic -- per Pitfall 5). Include helpful comments explaining each section. Contents:

    ```yaml
    # BookForge Style Guide — Korean Cute Watercolor
    # Used by: bookforge illustrate, bookforge build
    name: korean-cute-watercolor
    version: 1

    art_style:
      prompt_prefix: "Children's book illustration, soft watercolor style, warm pastel colors, gentle brushstrokes, hand-illustrated look, picture book quality"
      negative_prompt: "photorealistic, 3D render, dark, scary, violent, text, watermark, signature, low quality"
      color_palette:
        - "#F4E4C1"   # warm cream
        - "#A8D8EA"   # soft sky blue
        - "#FFB7B2"   # gentle coral
        - "#B5EAD7"   # mint green
        - "#E2C2F0"   # light lavender
        - "#FFDAC1"   # peach

    image:
      provider: flux_kontext_pro
      width: 1024
      height: 1024

    characters:
      ho-rang:
        name_en: Ho-rang
        name_ko: 호랑
        description: "A small, friendly tiger cub with round orange fur, dark stripes, big curious eyes, and a perpetually wagging tail. Wears a tiny red scarf."
        reference_image: characters/horang-ref.png
      gom-i:
        name_en: Gom-i
        name_ko: 곰이
        description: "A chubby, gentle bear cub with soft brown fur, round ears, warm brown eyes, and a shy smile. Wears a small blue bow on one ear."
        reference_image: characters/gomi-ref.png

    layout:
      trim_inches: [8.5, 8.5]
      bleed_inches: 0.125
      safe_margin_inches: 0.25
      dpi: 300
    ```

    3. Verify the default style guide loads without error:
       `uv run python -c "from bookforge.style.loader import load_style_guide; from pathlib import Path; sg = load_style_guide(Path('style-guides/korean-cute-watercolor.yaml')); print(f'Loaded: {sg.name}, {len(sg.characters)} characters')"`
  </action>
  <verify>
    <automated>uv run python -c "from bookforge.style.loader import load_style_guide; from pathlib import Path; sg = load_style_guide(Path('style-guides/korean-cute-watercolor.yaml')); assert len(sg.characters) == 2; print('OK')"</automated>
  </verify>
  <done>
    - style-guides/korean-cute-watercolor.yaml exists with Ho-rang and Gom-i character definitions
    - style-guides/characters/ directory exists for future reference images
    - Default style guide loads and validates without error
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_style_loader.py -x -v` -- all tests green
- `uv run pytest tests/ -x -q` -- full suite still passes
- Default style guide loads: `uv run python -c "from bookforge.style.loader import load_style_guide; ..."`
</verification>

<success_criteria>
- load_style_guide() correctly loads ruamel.yaml and validates through Pydantic
- Default style guide YAML has complete Ho-rang and Gom-i character definitions
- build_prompt_prefix() assembles the full image generation prefix
- Characters directory scaffolded for Phase 2 reference image generation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
