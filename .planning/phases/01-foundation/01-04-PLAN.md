---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - bookforge/story/generator.py
  - bookforge/cli/new.py
  - bookforge/cli/__init__.py
  - tests/test_generator.py
  - tests/test_new_command.py
autonomous: true
requirements:
  - CLI-01
  - STOR-05

must_haves:
  truths:
    - "User can run uv run bookforge new <slug> --prompt '...' and get a scaffolded book directory"
    - "Claude API is called with streaming to generate a bilingual story draft"
    - "Generated story.md is written in canonical format with YAML frontmatter and page headers"
    - "Book directory contains story.md, state.json, and subdirectories (images/, dist/, publish/)"
    - "Command fails gracefully if book directory already exists"
    - "Command fails gracefully if ANTHROPIC_API_KEY is not set"
  artifacts:
    - path: "bookforge/story/generator.py"
      provides: "generate_story() function calling Claude API with streaming"
      exports: ["generate_story"]
      min_lines: 30
    - path: "bookforge/cli/new.py"
      provides: "new_command() Typer command implementation"
      exports: ["new_command"]
      min_lines: 30
    - path: "tests/test_generator.py"
      provides: "Mocked Claude API test for story generation"
    - path: "tests/test_new_command.py"
      provides: "Integration test for bookforge new command"
  key_links:
    - from: "bookforge/cli/__init__.py"
      to: "bookforge/cli/new.py"
      via: "app.command('new')(new_command)"
      pattern: "app.command.*new"
    - from: "bookforge/cli/new.py"
      to: "bookforge/story/generator.py"
      via: "calls generate_story()"
      pattern: "from bookforge.story.generator import"
    - from: "bookforge/cli/new.py"
      to: "bookforge/state.py"
      via: "calls save_state() for initial state"
      pattern: "from bookforge.state import"
    - from: "bookforge/story/generator.py"
      to: "anthropic"
      via: "Anthropic().messages.stream()"
      pattern: "anthropic.Anthropic"
---

<objective>
Implement the `bookforge new <slug>` CLI command that scaffolds a book directory, calls Claude to generate a bilingual story draft, writes story.md, copies style guide reference, and initializes state.json.

Purpose: This is the user-facing entry point -- the first command anyone runs. It wires together the generator, state module, and style guide into a working end-to-end flow.
Output: Working `uv run bookforge new <slug> --prompt "..."` command with mocked tests.
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plans 01-03 -->

From bookforge/story/schema.py:
```python
class Book(BaseModel):
    meta: BookMeta
    pages: list[Page]

class BookMeta(BaseModel):
    title: str
    title_ko: str
    slug: str
    trim_size: str = "8.5x8.5"
    price: float
    ages: str
    style_guide: str
```

From bookforge/style/schema.py:
```python
class StyleGuide(BaseModel):
    name: str
    characters: dict[str, CharacterDef]
    def build_prompt_prefix(self) -> str: ...
```

From bookforge/style/loader.py:
```python
def load_style_guide(path: Path) -> StyleGuide: ...
```

From bookforge/story/parser.py:
```python
def parse_story(path: str | Path) -> Book: ...
```

From bookforge/state.py:
```python
def load_state(book_dir: Path) -> dict: ...
def save_state(book_dir: Path, state: dict) -> None: ...
```

From bookforge/config.py:
```python
def get_anthropic_key() -> str: ...
# BOOKFORGE_MODEL env var with default
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Claude story generator with streaming</name>
  <files>bookforge/story/generator.py, tests/test_generator.py</files>
  <behavior>
    - Test: generate_story() returns a string containing YAML frontmatter delimiters (---)
    - Test: generate_story() returns a string containing "## Page 1"
    - Test: generate_story() returns a string containing ko comment blocks
    - Test: generate_story() returns a string containing image comment
    - Test: generate_story() uses streaming (mock verifies .stream() was called, not .create())
    - Test: generate_story() passes style_guide_name in the user message
    - Test: generate_story() uses model from BOOKFORGE_MODEL env var (or default)
  </behavior>
  <action>
    Create bookforge/story/generator.py following RESEARCH.md Pattern 7:
    - SYSTEM_PROMPT constant instructing Claude to write in canonical story.md format
    - generate_story(prompt: str, style_guide_name: str, page_count: int = 12, ages: str = "4-8") -> str
    - Uses anthropic.Anthropic() (reads ANTHROPIC_API_KEY from env automatically)
    - Uses client.messages.stream() context manager (NOT .create() -- see anti-patterns)
    - Model from config (BOOKFORGE_MODEL env var, default claude-sonnet-4-5)
    - max_tokens=4096 (sufficient for 12-page story)
    - Returns stream.get_final_text()

    Create tests/test_generator.py:
    - Mock the anthropic client using unittest.mock (patch anthropic.Anthropic)
    - Provide a mock stream that returns a sample story.md string
    - Verify streaming was used, correct model passed, system prompt contains format instructions
    - Do NOT hit live API in tests
  </action>
  <verify>
    <automated>uv run pytest tests/test_generator.py -x -v</automated>
  </verify>
  <done>generate_story() calls Claude with streaming and returns story text; all tests pass with mocked API</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bookforge new CLI command</name>
  <files>bookforge/cli/new.py, bookforge/cli/__init__.py, tests/test_new_command.py</files>
  <behavior>
    - Test: `bookforge new test-book --prompt "A story"` creates books/test-book/ directory
    - Test: Created directory contains story.md, state.json, images/, dist/, publish/
    - Test: state.json contains correct slug, style_guide, and stage statuses
    - Test: Running new on existing slug prints error and exits with code 1
    - Test: Command registers correctly -- `bookforge --help` shows "new" subcommand
  </behavior>
  <action>
    1. Create bookforge/cli/new.py following RESEARCH.md Pattern 8:
       - new_command(slug, prompt, style_guide="korean-cute-watercolor", pages=12) with Typer annotations
       - Check if books/<slug>/ already exists -> error + exit(1)
       - Create directory tree: books/<slug>/{images, dist, publish}
       - Call generate_story(prompt, style_guide, page_count=pages) -> write to books/<slug>/story.md
       - Call save_state() with initial state dict (version, slug, style_guide, stages all pending except story=complete)
       - Print success message with next steps
       - Use Rich console for styled output (progress indicator during generation)

    2. Update bookforge/cli/__init__.py:
       - Import new_command from bookforge.cli.new
       - Register: app.command("new")(new_command)

    3. Create tests/test_new_command.py:
       - Use typer.testing.CliRunner to invoke the command
       - Mock generate_story to return a sample story.md string (no live API calls)
       - Test directory creation, file contents, error on duplicate slug
       - Use monkeypatch to set cwd to tmp_path so books/ is created in tmp
  </action>
  <verify>
    <automated>uv run pytest tests/test_new_command.py -x -v</automated>
  </verify>
  <done>
    - `uv run bookforge new` command works end-to-end (with mocked Claude)
    - Creates correct directory structure with story.md and state.json
    - Handles error cases (existing dir, missing API key)
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_generator.py tests/test_new_command.py -x -v` -- all tests green
- `uv run pytest tests/ -x -v` -- full suite passes (no regressions from Plans 01-03)
- `uv run bookforge --help` shows "new" subcommand
- `uv run bookforge new --help` shows slug, --prompt, --style, --pages options
</verification>

<success_criteria>
- `bookforge new <slug> --prompt "..."` scaffolds complete book directory
- Claude story generation uses streaming with mocked tests
- state.json initialized correctly
- Error handling for existing directory and missing API key
- All tests pass across entire suite
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
