---
phase: 02-image-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bookforge/images/__init__.py
  - bookforge/images/provider.py
  - bookforge/images/providers/__init__.py
  - bookforge/images/providers/flux_kontext.py
  - tests/test_image_provider.py
  - tests/test_flux_provider.py
autonomous: true
requirements: [IMG-01, IMG-02, IMG-09]

must_haves:
  truths:
    - "get_provider('flux_kontext_pro') returns a ReplicateFluxKontextProvider instance"
    - "Provider.generate() calls Replicate async_run with correct input_image, prompt, aspect_ratio, output_format"
    - "When model_version is set, the model ref includes the pinned SHA hash"
    - "Multi-character reference images are stitched side-by-side into a single PNG before API call"
    - "Single reference image is passed directly as a Path (no stitching)"
    - "FileOutput.aread() bytes are returned for the caller to save"
  artifacts:
    - path: "bookforge/images/provider.py"
      provides: "ImageProvider ABC, ImageRequest, ImageResult dataclasses"
      exports: ["ImageProvider", "ImageRequest", "ImageResult"]
    - path: "bookforge/images/providers/flux_kontext.py"
      provides: "ReplicateFluxKontextProvider with reference image stitching"
      exports: ["ReplicateFluxKontextProvider"]
    - path: "bookforge/images/providers/__init__.py"
      provides: "get_provider() factory function"
      exports: ["get_provider"]
  key_links:
    - from: "bookforge/images/providers/__init__.py"
      to: "bookforge/images/providers/flux_kontext.py"
      via: "get_provider factory returns ReplicateFluxKontextProvider"
      pattern: "get_provider.*flux_kontext"
    - from: "bookforge/images/providers/flux_kontext.py"
      to: "replicate.async_run"
      via: "SDK async_run with Path input for auto-upload"
      pattern: "replicate\\.async_run"
---

<objective>
Create the image provider abstraction layer and Flux Kontext Pro concrete implementation.

Purpose: Establish the provider-agnostic interface (IMG-02) and wire up Replicate's Flux Kontext Pro as the default provider (IMG-01) with version pinning (IMG-09). This is the foundation all image generation flows through.

Output: ImageProvider ABC, ImageRequest/ImageResult dataclasses, ReplicateFluxKontextProvider with multi-character stitching, get_provider() factory, and full test coverage.
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-image-generation/02-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From bookforge/style/schema.py:
```python
class ImageConfig(BaseModel):
    provider: str = "flux_kontext_pro"
    width: int = 1024
    height: int = 1024

class CharacterDef(BaseModel):
    name_en: str
    name_ko: str
    description: str
    reference_image: str

class StyleGuide(BaseModel):
    name: str
    version: int = 1
    art_style: ArtStyle
    image: ImageConfig = ImageConfig()
    characters: dict[str, CharacterDef]
    layout: Layout = Layout()

    def build_prompt_prefix(self) -> str: ...
```

From bookforge/state.py:
```python
def load_state(book_dir: Path) -> dict: ...
def save_state(book_dir: Path, state: dict) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ImageProvider ABC, dataclasses, and factory with tests</name>
  <files>bookforge/images/__init__.py, bookforge/images/provider.py, bookforge/images/providers/__init__.py, tests/test_image_provider.py</files>
  <behavior>
    - test_image_request_defaults: ImageRequest with only prompt+reference_images has width=1024, height=1024, output_format="png", seed=None, model_version=None
    - test_image_result_fields: ImageResult stores path, provider, model_version, seed
    - test_provider_abc_not_instantiable: ImageProvider() raises TypeError (abstract)
    - test_get_provider_flux: get_provider("flux_kontext_pro") returns ReplicateFluxKontextProvider instance
    - test_get_provider_unknown: get_provider("unknown") raises ValueError
  </behavior>
  <action>
    Create `bookforge/images/__init__.py` (empty).

    Create `bookforge/images/provider.py` with:
    - `ImageRequest` dataclass: prompt (str), reference_images (list[Path]), width (int=1024), height (int=1024), output_format (str="png"), seed (int|None=None), model_version (str|None=None)
    - `ImageResult` dataclass: path (Path), provider (str), model_version (str), seed (int|None=None)
    - `ImageProvider` ABC with: abstract async method `generate(self, request: ImageRequest) -> ImageResult`, abstract property `provider_name -> str`

    Create `bookforge/images/providers/__init__.py` with:
    - `get_provider(name: str, model_version: str|None = None) -> ImageProvider` factory function
    - Maps "flux_kontext_pro" to ReplicateFluxKontextProvider(model_version=model_version)
    - Raises ValueError for unknown provider names

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>uv run pytest tests/test_image_provider.py -x -v</automated>
  </verify>
  <done>ImageProvider ABC exists with generate() and provider_name. ImageRequest/ImageResult dataclasses validated. get_provider() factory returns correct provider for "flux_kontext_pro" and raises on unknown. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReplicateFluxKontextProvider with reference stitching and version pinning</name>
  <files>bookforge/images/providers/flux_kontext.py, tests/test_flux_provider.py</files>
  <behavior>
    - test_flux_generate_single_ref: With 1 reference image, calls replicate.async_run with input_image=Path (no stitching), correct prompt, aspect_ratio="1:1", output_format="png"
    - test_flux_generate_multi_ref: With 2 reference images, stitches side-by-side into a temp PNG, passes that temp Path as input_image
    - test_flux_generate_no_ref: With 0 reference images, does not include input_image in inputs
    - test_flux_pinned_version: When model_version="sha256:abc", model ref is "black-forest-labs/flux-kontext-pro:sha256:abc"
    - test_flux_unpinned_version: When model_version=None, model ref is "black-forest-labs/flux-kontext-pro"
    - test_flux_provider_name: provider_name property returns "replicate/flux-kontext-pro"
    - test_flux_reads_output_bytes: generate() calls FileOutput.aread() and writes bytes to output_path, returns ImageResult
  </behavior>
  <action>
    Create `bookforge/images/providers/flux_kontext.py` with:
    - `ReplicateFluxKontextProvider(ImageProvider)`:
      - `__init__(self, model_version: str|None = None)` storing version
      - `provider_name` property returns "replicate/flux-kontext-pro"
      - `_model_ref(self) -> str` returning MODEL or MODEL:hash
      - `async generate(self, request: ImageRequest) -> ImageResult`:
        - Call `_prepare_reference_image(request.reference_images)` to handle 0/1/multi refs
        - Build inputs dict with prompt, aspect_ratio (derive from width/height — use "1:1" for square), output_format
        - If ref_image exists, add input_image=ref_image (Path object)
        - If request.seed is not None, add seed
        - Call `await replicate.async_run(self._model_ref(), input=inputs)`
        - Handle FileOutput: `await file_output.aread()` to get bytes
        - Write bytes to request output path, return ImageResult
      - `MODEL = "black-forest-labs/flux-kontext-pro"` class constant
    - `_prepare_reference_image(refs: list[Path]) -> Path | None`:
      - 0 refs: return None
      - 1 ref: return refs[0]
      - 2+ refs: Pillow side-by-side stitch into tempfile, return Path
    - `_aspect_ratio(width: int, height: int) -> str`:
      - Map common ratios to Flux aspect_ratio strings. Default "1:1" for square.

    Mock `replicate.async_run` in tests using `unittest.mock.AsyncMock`. Create a mock `FileOutput` that has an `aread()` coroutine returning fake PNG bytes. Use small real PNG files from Pillow `Image.new()` for reference image stitching tests.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>uv run pytest tests/test_flux_provider.py -x -v</automated>
  </verify>
  <done>ReplicateFluxKontextProvider correctly calls replicate.async_run with proper inputs. Single ref passed as Path, multi-ref stitched via Pillow. Version pinning works. All tests green. Full provider suite green: `uv run pytest tests/test_image_provider.py tests/test_flux_provider.py -x -v`</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_image_provider.py tests/test_flux_provider.py -x -v
</verification>

<success_criteria>
- ImageProvider ABC with generate() and provider_name abstract members
- ImageRequest/ImageResult dataclasses with all fields from research
- ReplicateFluxKontextProvider calling replicate.async_run with correct inputs
- Multi-character reference stitching via Pillow
- Version pinning via model ref string
- get_provider() factory working for "flux_kontext_pro"
- All tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/02-image-generation/02-01-SUMMARY.md`
</output>
