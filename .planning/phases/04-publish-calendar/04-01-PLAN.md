---
phase: 04-publish-calendar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bookforge/review.py
  - bookforge/cli/review.py
  - bookforge/cli/__init__.py
  - tests/test_review.py
  - tests/test_review_cli.py
autonomous: true
requirements: [CLI-04, REV-01, REV-02, REV-03]

must_haves:
  truths:
    - "User can run `uv run bookforge review <slug>` and see page count, image count, word counts EN/KR, and file sizes"
    - "Review prints a checklist of items to verify (story quality, Korean proofread, image consistency, cover strength)"
    - "Review prompts for explicit y/n approval and stamps result into state.json"
    - "Re-running review after approval shows the existing approval status"
  artifacts:
    - path: "bookforge/review.py"
      provides: "Review logic: gather_summary(), print_checklist(), stamp_approval()"
      exports: ["gather_summary", "REVIEW_CHECKLIST"]
    - path: "bookforge/cli/review.py"
      provides: "CLI review command with Typer"
      exports: ["review_command"]
    - path: "tests/test_review.py"
      provides: "Unit tests for review logic"
    - path: "tests/test_review_cli.py"
      provides: "CLI integration tests for review command"
  key_links:
    - from: "bookforge/cli/review.py"
      to: "bookforge/review.py"
      via: "imports gather_summary, REVIEW_CHECKLIST"
      pattern: "from bookforge\\.review import"
    - from: "bookforge/cli/review.py"
      to: "bookforge/state.py"
      via: "load_state/save_state for approval stamp"
      pattern: "from bookforge\\.state import"
    - from: "bookforge/cli/__init__.py"
      to: "bookforge/cli/review.py"
      via: "app.command registration"
      pattern: "app\\.command.*review"
---

<objective>
Implement the `bookforge review` command that displays a book summary (page count, image count, word counts, file sizes), prints a manual checklist, and prompts for y/n approval that gets stamped into state.json.

Purpose: Gate that prevents publishing unreviewed books -- must pass before `publish` command will run.
Output: Working `uv run bookforge review <slug>` command with tests.
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From bookforge/story/schema.py:
```python
class BilingualText(BaseModel):
    en: str
    ko: str

class Page(BaseModel):
    number: int
    text: BilingualText
    image_prompt: str
    image_override: str | None = None

class BookMeta(BaseModel):
    title: str
    title_ko: str
    slug: str
    trim_size: str = "8.5x8.5"
    price: float
    ages: str
    style_guide: str

class Book(BaseModel):
    meta: BookMeta
    pages: list[Page]
```

From bookforge/state.py:
```python
def load_state(book_dir: Path) -> dict
def save_state(book_dir: Path, state: dict) -> None
```

From bookforge/story/parser.py:
```python
def parse_story(path: Path) -> Book
```

From bookforge/cli/__init__.py (registration pattern):
```python
app.command("build")(build_command)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Review logic module with summary gathering and checklist</name>
  <files>bookforge/review.py, tests/test_review.py</files>
  <behavior>
    - gather_summary(book, book_dir) returns dict with: page_count (int), image_count (int from glob), word_count_en (int), word_count_ko (int), pdf_files (list of dicts with name + size_kb)
    - Word count: split on whitespace for English text across all pages; split on whitespace for Korean text across all pages
    - Image count: glob for PNG files in book_dir/images/ matching page-*.png pattern
    - PDF files: glob book_dir/output/*.pdf, return name and size in KB
    - REVIEW_CHECKLIST is a list of strings: ["Story reads naturally in English", "Korean translation proofread by native speaker", "All illustrations show consistent character design", "Cover image is strong and eye-catching", "No placeholder text or TODO markers remain"]
    - Edge case: book with no images yet returns image_count=0
    - Edge case: book with no PDFs yet returns empty pdf_files list
  </behavior>
  <action>
    Create bookforge/review.py with:
    1. `gather_summary(book: Book, book_dir: Path) -> dict` -- counts pages from book.pages, globs images/page-*.png for image count, sums len(page.text.en.split()) and len(page.text.ko.split()) across all pages for word counts, globs output/*.pdf for file sizes
    2. `REVIEW_CHECKLIST: list[str]` -- constant list of 5 checklist items (story quality, Korean proofread, image consistency, cover strength, no placeholders)
    3. `format_summary(summary: dict) -> str` -- formats the summary dict into a Rich-compatible string using rich.table.Table for the stats and a bullet list for PDF files

    Create tests/test_review.py with TDD tests:
    - Test gather_summary with a 2-page book fixture, some images, and some PDFs in tmp_path
    - Test gather_summary with empty images/output dirs
    - Test word counting accuracy
    - Test REVIEW_CHECKLIST has exactly 5 items
  </action>
  <verify>
    <automated>cd /home/jeff/Projects/k-kids-books && uv run pytest tests/test_review.py -x -v</automated>
  </verify>
  <done>gather_summary returns correct counts for pages, images, words, PDFs; REVIEW_CHECKLIST has 5 items; all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI review command with approval prompt and state stamping</name>
  <files>bookforge/cli/review.py, bookforge/cli/__init__.py, tests/test_review_cli.py</files>
  <behavior>
    - `uv run bookforge review <slug>` prints summary table, checklist, then prompts "Approve this book for publishing? [y/N]:"
    - Answering "y" stamps state.json with review_approved: true, review_date: ISO datetime, review_summary: {the summary dict}
    - Answering "n" or anything else prints "Review not approved." and exits 0 (not an error)
    - If book already approved, prints "Already approved on {date}" and the summary, does NOT re-prompt
    - If book_dir does not exist, prints error and exits 1
  </behavior>
  <action>
    Create bookforge/cli/review.py with:
    1. `review_command(slug: str)` -- loads book via parse_story, calls gather_summary, prints Rich table with summary stats (Page count, Image count, EN word count, KR word count), prints each PDF with size, prints numbered checklist items, checks state for existing approval (if review_approved is True, show status and return), prompts with typer.confirm("Approve this book for publishing?", default=False), on approval: save_state with review_approved=True, review_date=datetime.now().isoformat(), review_summary=summary dict
    2. Use Rich console for output (from rich.console import Console; from rich.table import Table)
    3. Register in bookforge/cli/__init__.py: import review_command, add `app.command("review")(review_command)`

    Create tests/test_review_cli.py:
    - Test with CliRunner: successful review flow with "y" input produces state.json with review_approved
    - Test with "n" input: no approval in state
    - Test already-approved book shows status without prompting
    - Test missing book directory gives exit code 1
    - Use tmp_path with sample story.md, mock or create minimal book dir structure
  </action>
  <verify>
    <automated>cd /home/jeff/Projects/k-kids-books && uv run pytest tests/test_review_cli.py -x -v</automated>
  </verify>
  <done>CLI review command prints summary + checklist, prompts for approval, stamps state.json on "y", shows existing approval on re-run; all tests pass</done>
</task>

</tasks>

<verification>
```bash
cd /home/jeff/Projects/k-kids-books && uv run pytest tests/test_review.py tests/test_review_cli.py -x -v
```
</verification>

<success_criteria>
- `uv run bookforge review <slug>` displays page count, image count, EN/KR word counts, PDF file sizes
- Checklist of 5 review items printed
- y/n approval prompt stamps state.json with review_approved, review_date
- Already-approved books show status without re-prompting
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-publish-calendar/04-01-SUMMARY.md`
</output>
